import React from 'react';

export default class Clock extends React.Component {

    // TODO
    //
    //  o Create a state object, which contains a `Date` attribute.
    //
    //  o Initialize it with the current date and time.
    //
    //  o Let the component render the current time.
    //
    //    Hint: Date.toLocaleTimeString()
    //
    //  o Start a timer, which updates the time every second.
    //
    //    Hint: Should happen, when the component is mounted.
    //
    //  o Stop the timer, when the component is unmounted.
    //

    render() {

        return '';

    }

}
